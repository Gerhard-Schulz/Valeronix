@model IEnumerable<MonitorMagaz>

@{
    ViewData["Title"] = "Мониторы";
}

<div class="row mt-3">
    <div class="col-3 text-center">
        <h2 class="text-primary mt-4">Мониторы <a asp-controller="Monitor" asp-action="Add" class="btn btn-sm btn-outline-primary mt-3"><i class="bi bi-plus-lg"></i> Новый манитор</a></h2>
    </div>
    <div class="col-9">
        <table class="table table-hover w-100 mt-3 pb-2 cell-centered">
            <tbody class="cell-centered mt-5">
                @foreach (var monitor in Model)
                {
                    <tr>
                        <td>
                            <h5>@monitor.ModelOfDevice.Creator.Name @monitor.ModelOfDevice.Name</h5>
                        </td>
                        <td>
                            <h5>@monitor.TypeOfMonitor</h5>
                        </td>
                        <td>
                            <h5>@monitor.Price руб.</h5>
                        </td>
                        <td>
                            <div class="btn-group btn-group-sm w-100" role="group">
                                <a asp-controller="Monitor" asp-action="Edit" asp-route-id="@monitor.Id" class="btn btn-sm btn-outline-warning"><i class="bi bi-pen"></i> Изменить</a>
                                <a asp-controller="Monitor" asp-action="Delete" asp-route-id="@monitor.Id" class="btn btn-sm btn-outline-danger"><i class="bi bi-trash3"></i> Удалить</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
